# Independent validation data

Use newly acquired source groups that have no training/development lineage.
Human ground-truth grades must be entered without seeing system predictions.
This split may support predeclared development decisions, but never a final
locked-performance claim.
