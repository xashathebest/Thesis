# Locked test data

Keep this split inaccessible to model/threshold selection. It must use unseen
physical fish and acquisition groups where possible, plus independently entered
ground truth. Run the locked evaluation only after the study protocol is frozen.
