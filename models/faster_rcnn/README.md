# Faster R-CNN with ResNet50

This folder is reserved for Faster R-CNN with ResNet50 training outputs, checkpoints, and evaluation artifacts.

Keep this branch isolated from YOLOv8-nano and EfficientDet-D3 outputs.

TODO: Add training, validation, and export outputs for Faster R-CNN with ResNet50.
