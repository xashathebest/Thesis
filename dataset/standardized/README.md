# Standardized Images

Derived, reproducible working-resolution images are written here. Every run must
record source hashes, output dimensions, format, quality, and preprocessing
parameters. Do not repeatedly recompress JPEG files.
