# Instance and Attribute Annotations

The final class is recorded per fish, not inherited blindly from the raw folder.
Use at least two qualified annotators where possible and preserve disagreements
until adjudication.

Subdirectories:

- `instances/`: polygons or masks for individual fish
- `attributes/`: independent quality attributes
- `adjudication/`: expert decisions and disagreement resolution
