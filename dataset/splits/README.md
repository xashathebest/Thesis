# Group-Safe Dataset Splits

Materialized `train`, `validation`, and `test` data may be generated here only
from an approved split manifest. A specimen, scene/near-duplicate group, and
collection batch must not cross split boundaries.
