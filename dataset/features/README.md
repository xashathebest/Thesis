# Engineered Features

Feature tables must contain one row per reviewed, gradable fish instance plus
`instance_id`, `specimen_id` or approved `group_id`, `batch_id`, `final_class`,
and `split`. Feature normalization is fitted on training rows only.

The combined table expected by the feature-model baseline is:

`combined_features.csv`
