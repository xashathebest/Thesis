# Cleaned Dataset View

This is a derived canonical view, never a replacement for `dataset/raw/`.
Prefer a manifest of `KEEP`, `EXCLUDE_FROM_TRAINING`, `DUPLICATE`,
`NEAR_DUPLICATE`, `MANUAL_REVIEW`, `INVALID`, `OCCLUDED`, and `TRUNCATED`
statuses rather than copying or deleting source images.
