"""Read-only dataset audit stages.

The modules in this package inspect ``dataset/raw`` and only write derived
manifests or reports outside that directory. Import functions from their
individual modules so each module also remains cleanly runnable with ``-m``.
"""
